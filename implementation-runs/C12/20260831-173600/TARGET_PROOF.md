# TARGET_PROOF: C12 Real Wealthfolio Product POC Correction

```yaml
target_product: "Wealthfolio"
exact_version: "3.7.0"
installation_source: "https://github.com/wealthfolio/wealthfolio/releases/download/v3.7.0/Wealthfolio_3.7.0_x64-setup.exe"
real_import_interface: "Wealthfolio Desktop UI Activities -> Import CSV with native CSV column format (date,symbol,instrumentType,quantity,activityType,unitPrice,currency,fee,amount,fxRate,subtype)"
real_export_backup_interface: "Wealthfolio Desktop UI Settings -> Exports (Full Database SQLite app.db, CSV, JSON)"
real_restore_interface: "Database file replacement at %APPDATA%\\com.teymz.wealthfolio\\app.db or UI Settings Backup & Restore"
mcp_status: "SUPPORTED"
real_mcp_interface: "Built-in Desktop MCP Server at http://127.0.0.1:<port>/mcp with scoped Personal Access Token (PAT)"
independent_oracle: "C11 canonical fixture/control values (tests/fixtures/golden_broker_expected.json and golden_broker_export.csv)"
facade_failure_example: "Writing Python functions mcp_read_holdings() or generate_backup_export() in ipos/portfolio/wealthfolio.py that return synthetic dicts without running the real Wealthfolio application."
```

## Concrete Proof Action
The C11 validated canonical activities CSV is generated and imported into the actual Wealthfolio v3.7.0 desktop application via its official CSV import wizard. Wealthfolio parses and displays the portfolio positions (`SPY`: 10 units, `AAPL`: 3 units in `EUR`), and generates a real application backup/export (`app.db` or export CSV). The exported data is reconciled against C11 independent golden control values. Furthermore, the built-in MCP server is enabled with a read-only PAT and queried via real HTTP/SSE MCP client to retrieve holdings.

---

## 1. Product Identification & Official Interfaces
- **Target Application**: Wealthfolio Desktop (Windows x64)
- **Version**: `v3.7.0` (latest release)
- **Publisher / Repository**: `https://github.com/wealthfolio/wealthfolio` / `https://wealthfolio.app`
- **Supported Local Execution**: Windows desktop application (`Wealthfolio_3.7.0_x64-setup.exe` or portable unpack)
- **Supported Import Format**: Native CSV with columns:
  `date,symbol,instrumentType,quantity,activityType,unitPrice,currency,fee,amount,fxRate,subtype`
- **Supported Export Format**: Full SQLite database (`app.db`), CSV activities export, JSON export via `Settings → Exports`
- **Supported Restore**: Restoring `app.db` SQLite database to `%APPDATA%\com.teymz.wealthfolio\app.db`
- **Supported Native MCP**: Built-in Model Context Protocol server enabled via `Settings → AI Agent Access`, authenticated with scoped Personal Access Tokens.

---

## 2. Independent Ground Truth Oracle (C11)
Expected portfolio state from `tests/fixtures/golden_broker_expected.json`:
- **Holdings**:
  - `SPY` (ISIN: `US7846721097`): Quantity = `10.0`, Book Cost Basis = `5005.00 EUR` (Unit: `500.50 EUR`)
  - `AAPL` (ISIN: `US0378331005`): Quantity = `3.0` (Bought 5 - Sold 2), Book Cost Basis = `540.90 EUR` (Unit: `180.30 EUR`)
- **Activity Totals**:
  - 4 transactions (2 BUY, 1 DIVIDEND, 1 SELL)
  - Gross Buys: `5900.00 EUR`
  - Gross Sells: `380.00 EUR`
  - Gross Dividends: `15.00 EUR`
  - Total Fees: `8.00 EUR`
  - Total Taxes: `7.25 EUR`
  - Net Cash Flow: `-5520.25 EUR`

---

## 3. Anti-Facade Invariants
1. `ipos/portfolio/wealthfolio.py` must strictly be a deterministic C11 -> Wealthfolio Native CSV format converter. It must NEVER contain fake MCP handlers or fake backup export generators.
2. Direct SQLite database writes/injection before import are strictly FORBIDDEN. All data enters Wealthfolio through its supported CSV import surface.
3. Proof evidence must consist of actual files generated by Wealthfolio (`app.db`, exported CSVs, MCP tool responses, screenshots/UI summaries), not Python return values.
4. If native MCP server cannot be connected or enabled due to local environment constraints, it must be marked `DEFERRED` / `PASS_WITH_LIMITATIONS` rather than faked with local Python functions.
