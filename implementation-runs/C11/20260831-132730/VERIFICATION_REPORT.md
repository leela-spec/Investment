# Independent Adversarial Verification Report: IPOS Correction Module C11

## 1. Verdict
**VERDICT: `PASS_WITH_LIMITATIONS`**
*Approved Limitation*: In accordance with [C11_NORMALIZER_CORRECTION.yaml](file:///c:/GitDev/Investment/05_blueprint/research/2026-08-28-modular-rebuild/antigravity-v2/C11_NORMALIZER_CORRECTION.yaml) Step 8 and Pass Condition 6, real ZERO / Smartbroker broker-specific PDF/CSV adapters remain explicitly deferred/blocked pending representative external broker export files. All core normalizer accounting, multi-currency segregation, independent reconciliation oracles, CLI exit code semantics, and stop-gate hook integrity are fully implemented, independently tested, and verified.

---

## 2. Claim Under Test
Correction C11 repairs M11 canonical transaction accounting, independent reconciliation, multi-currency handling, and stop-hook robustness:
1. **Stop-hook state integrity**: [`.agents/hooks/ipos_stop_gate.py`](file:///c:/GitDev/Investment/.agents/hooks/ipos_stop_gate.py) handles UTF-8-SIG (BOM), fails closed on malformed task JSON (broken syntax or non-dict root), and accepts `active: false`.
2. **Multi-currency accounting**: [`PortfolioNormalizer.normalize_csv_fixture`](file:///c:/GitDev/Investment/ipos/portfolio/normalizer.py) segregates totals per currency into `summary_totals_by_currency` and never sums across currencies without conversion.
3. **Reconcile CLI exit semantics**: [`ipos.portfolio.cli`](file:///c:/GitDev/Investment/ipos/portfolio/cli.py) exits `0` on `BALANCED`, `1` on `MISMATCH`, and `2` on `UNVERIFIABLE_NO_SOURCE_CONTROL`.
4. **Full control-oracle reconciliation**: Reconciles net cash flow, component totals (`gross_buys`, `gross_sells`, `gross_dividends`, `total_fees`, `total_taxes`), and ending holdings (`quantity`, `cost_basis`, `as_of`). Any component/holding mismatch yields `MISMATCH` even when net cash matches.
5. **Valuation timestamp semantics**: Holding `as_of` timestamp is updated only on valuation trade events (`BUY`, `SELL`) and is not advanced on `DIVIDEND`.

---

## 3. Real Target-System Proof & Anti-Facade Audit
- **Named Target**: Deterministic accounting normalizer and reconciliation engine + Antigravity Stop Gate Hook.
- **Participation Check**:
  - `ipos/portfolio/normalizer.py`: Direct implementation of canonical schema validation, activity lineage tracking, weighted-average book basis relief, per-currency aggregation, and multi-field control comparison.
  - `ipos/portfolio/cli.py`: Subprocess execution confirmed real normalization, parquet/csv serialization, and explicit exit codes (`0`, `1`, `2`).
  - `.agents/hooks/ipos_stop_gate.py`: Real process invocation under Python 3.12 verified stdin ingestion, UTF-8-SIG parsing, and fail-closed decisions.
- **Facade Elimination**:
  - `reconciliation_difference` is strictly derived by comparing actual normalized sums against control input.
  - `reconciliation_status` returns `UNVERIFIABLE_NO_SOURCE_CONTROL` when no control is supplied (refuses to self-validate).
  - Net cash matching alone is insufficient: component or holding discrepancies force `MISMATCH`.

---

## 4. Test-Oracle Independence Audit
- Ground truth is housed in external JSON fixtures ([`tests/fixtures/golden_broker_expected.json`](file:///c:/GitDev/Investment/tests/fixtures/golden_broker_expected.json), [`tests/fixtures/multi_currency_broker_expected.json`](file:///c:/GitDev/Investment/tests/fixtures/multi_currency_broker_expected.json), [`tests/fixtures/component_mismatch_control_expected.json`](file:///c:/GitDev/Investment/tests/fixtures/component_mismatch_control_expected.json), [`tests/fixtures/holding_mismatch_control_expected.json`](file:///c:/GitDev/Investment/tests/fixtures/holding_mismatch_control_expected.json)).
- Expected figures were manually computed from transaction math (`BUY: -(gross+fees+taxes)`, `SELL: +(gross-fees-taxes)`, `DIVIDEND: +(gross-fees-taxes)`), not generated by `PortfolioNormalizer`.

---

## 5. Required Steps Matrix

| Step | Description | Classification | Proof Evidence |
|---|---|---|---|
| Step 1 | Read M11 authority & C11; inspect code/fixtures | `DONE_PROVEN` | Inspected blueprint, codebase, and fixture directory |
| Step 2 | Create TARGET_PROOF.md with sign/cost conventions | `DONE_PROVEN` | [`TARGET_PROOF.md`](file:///c:/GitDev/Investment/implementation-runs/C11/20260831-132730/TARGET_PROOF.md) verified |
| Step 3 | Golden ledger fixture with manual expected totals | `DONE_PROVEN` | `golden_broker_export.csv` and `golden_broker_expected.json` |
| Step 4 | Define transaction sign semantics explicitly | `DONE_PROVEN` | Explicit sign handling in `normalizer.py` lines 240-279 |
| Step 5 | Weighted-average book cost-basis relief | `DONE_PROVEN` | Proportional relief on SELL verified in test `M11-T08` |
| Step 6 | Independent control reconciliation | `DONE_PROVEN` | Multi-field checks list with `diff` calculation |
| Step 7 | Inconsistent mismatch fixture | `DONE_PROVEN` | `mismatch_control_expected.json` yields `MISMATCH` |
| Step 8 | Defer ZERO/Smartbroker adapters | `DEFERRED_BY_APPROVED_WAIVER` | Explicitly deferred in state and report |
| Step 9 | Run CLI normalize/validate/reconcile deterministically | `DONE_PROVEN` | Tested CLI runs twice yielding identical SHA-256 |
| Step 10 | Invoke verification | `DONE_PROVEN` | 15/15 tests passing across test suites |

---

## 6. Pass-Condition Matrix

| Pass Condition | Named Target Actually Involved? | Independent Evidence? | Result |
|---|---|---|---|
| 1. Inconsistent fixture fails reconciliation | Yes (`PortfolioNormalizer`) | `test_m11_t06` & `test_m11_t11` assert `MISMATCH` | `PASS` |
| 2. Expected ledger balances fixed independently | Yes (JSON Fixtures) | `golden_broker_expected.json` manual values | `PASS` |
| 3. Cash signs & positions match ledger | Yes (`PortfolioNormalizer`) | `test_m11_t01`, `test_m11_t02`, `test_m11_t08` | `PASS` |
| 4. Zero silent drops | Yes (`PortfolioNormalizer`) | Manifest row count == activity count == 4 | `PASS` |
| 5. Real broker coverage status is truthful | Yes (`state.json`) | Explicit limitation documented | `PASS` |
| 6. Verifier verdict PASS or approved LIMITATIONS | Yes (Verification Suite) | `PASS_WITH_LIMITATIONS` approved in YAML | `PASS` |

---

## 7. Independent Reruns & Verification Evidence

### Pytest Execution
Command: `.venv\Scripts\pytest.exe tests/test_m11_normalizer.py tests/test_stop_gate.py -v`
```
tests/test_m11_normalizer.py::test_m11_t01_golden_csv_fixture_mapping_vs_independent_oracle PASSED
tests/test_m11_normalizer.py::test_m11_t02_text_fixture_preserves_values_and_cash_flow PASSED
tests/test_m11_normalizer.py::test_m11_t03_standalone_fee_tax_semantics PASSED
tests/test_m11_normalizer.py::test_m11_t04_reproducibility_determinism PASSED
tests/test_m11_normalizer.py::test_m11_t05_row_arithmetic_invalid_causes_validation_exception PASSED
tests/test_m11_normalizer.py::test_m11_t06_source_control_mismatch_fails_reconciliation PASSED
tests/test_m11_normalizer.py::test_m11_t07_unverifiable_without_source_control PASSED
tests/test_m11_normalizer.py::test_m11_t08_economic_book_cost_basis_relief_on_sales PASSED
tests/test_m11_normalizer.py::test_m11_t09_valuation_timestamp_semantics PASSED
tests/test_m11_normalizer.py::test_m11_t10_multi_currency_accounting PASSED
tests/test_m11_normalizer.py::test_m11_t11_full_control_oracle_component_and_holding_mismatch PASSED
tests/test_m11_normalizer.py::test_m11_t12_cli_exit_code_semantics PASSED
tests/test_stop_gate.py::test_stop_gate_inactive_task_allows_stop PASSED
tests/test_stop_gate.py::test_stop_gate_bom_task_file_parsed PASSED
tests/test_stop_gate.py::test_stop_gate_malformed_task_file_fails_closed PASSED
============================= 15 passed in 5.86s ==============================
```

### CLI Exit Codes
- `reconcile` with matching control -> Exit code `0` (`BALANCED`)
- `reconcile` with mismatch control -> Exit code `1` (`MISMATCH`)
- `reconcile` with no control -> Exit code `2` (`UNVERIFIABLE_NO_SOURCE_CONTROL`)
- `normalize` with no control -> Exit code `0` (with notice)

---

## 8. Negative and Anti-Facade Verification
1. **Stop Gate Non-Dict JSON**: Tested JSON root `[1, 2, 3]` -> Returns `{"decision": "continue", "reason": "Malformed .agents/current-task.json: root must be a JSON object. Fail closed."}`
2. **Stop Gate Empty JSON**: Tested empty string -> Returns `{"decision": "continue", ... Fail closed}`
3. **Oversell Rejection**: Attempting to sell 6 units when 5 owned raises `ValidationException: Oversell condition for instrument...`
4. **Duplicate Lineage Rejection**: Duplicate `source_row_id` raises `ValidationException: Duplicate source_row_id detected...`
5. **Component Mismatch with Matching Net Cash**: `component_mismatch_control_expected.json` (fees 99.0 vs 8.0) fails with `reconciliation_status: "MISMATCH"` and diff `91.0`.
6. **Holding Mismatch with Matching Net Cash**: `holding_mismatch_control_expected.json` (qty 99.0 vs 3.0) fails with `reconciliation_status: "MISMATCH"` and diff `96.0`.

---

## 9. Deviations & State-Integrity Failures
None detected.

---

## 10. Residual Risks
- Real Smartbroker and ZERO PDF/CSV broker export parsing will require subsequent adapter modules once genuine export data samples are provided.

---

## 11. Final Assessment
Module C11 fulfills all required correction criteria and is verified as **`PASS_WITH_LIMITATIONS`**.
