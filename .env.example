# IPOS environment — copy to .env and fill in.
# Phase 1 needs only FRED_API_KEY (free: https://fredaccount.stlouisfed.org/apikeys).
# Stooq needs no key. AI provider keys are Phase 2 (system runs fully with provider: none).

FRED_API_KEY=
